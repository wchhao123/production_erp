抓包，虽然说上个项目也用了但是经过了这个项目对与抓包的体会更加深刻了，本次项目抓包主要看的是请求网址和content-type
控制器Controller层在获得请求网址再经过逻辑处理，决定返回给用户在浏览器显示的信息，而content-type决定显示信息的类型
是json数据还是html。
增删改查，逆向工程大多数的功能都帮你写了，感觉大多数的增删改查调用mapper中的接口就好了，分页甚至都可以用pageHelper
也不用自己写了，验证器，本来我的物料监控里是没有验证器的，但是太少了又去写了人员监控的人工管理正好用到了，注意看响应的
参数100 200 500吧好像，其中身份证号需要判断，然后具体的老师课上都有讲。上传图片还在学习组长的代码。
github，慢慢用着用着就熟悉了，记得第一个下午光pull就一下午了，记住几个关键词就好了，远程仓库，本地仓库，暂存器
pull push commit 远程仓库->暂存器(idea)用的pull idea->本地仓库用的commit 本地仓库—>远程仓库用的是push。